# dependencias principais
Django==5.2.14

# driver postgresql 17 (psycopg3 com binarios pre-compilados — sem precisar de gcc)
psycopg[binary]>=3.1.0

# leitura de variaveis de ambiente a partir do arquivo .env
python-decouple>=3.8
